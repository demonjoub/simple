
const height = (height = 100) => ({
  color: 'red',
  height: height,
  width: 200
})

const widht = (widht = 100) => ({
  color: 'blue',
  height: 100,
  width: widht
})

const Styles = {
  height,
  widht
}

export default Styles
